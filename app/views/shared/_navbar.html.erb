<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <%= link_to 'Ofertas De Trabajo', root_path, class: 'navbar-brand text-white' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Ofertas', offers_path, class: 'nav-link text-white' %>
                </li>
                <li class="nav-item">
                    <% if user_signed_in? %>
                        <% if current_user.isadmin?%>
                            <li>
                                <%= link_to 'Crear usuario', '/admin/users/new', class: 'dropdown-item text-white'%>
                            </li>
                        <% end %>
                    <% end %>
                </li>
            </ul>
           
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <% if user_signed_in? %>
                    
                        <% if current_user.name %>
                            Hola <%= current_user.name %>
                            
                        <% else %>
                            Hola <%= current_user.email %>
                        <% end %>

                    <% else %>
                        Dale registrate o conectate
                    <% end %>
                    </a>
                    
                    <ul class="dropdown-menu">
                        <% if user_signed_in? %>
                            <li><%= link_to 'Editar perfil', edit_user_registration_path, class: 'dropdown-item' %></li>
                            <li><%= link_to 'Tus comentarios',  edit_user_password_path	, class: 'dropdown-item'%></li>
                            <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
                        <% else %>
                            <li><%= link_to 'Iniciar sesión',  new_user_session_path, class: 'dropdown-item'%></li>
                            <li><hr class="dropdown-divider"></li>
                            <%if User.count == 0%>
                                <li><%= link_to 'Registro',  new_user_registration_path, class: 'dropdown-item'%></li>
                            <% end %>
                        <% end %>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>